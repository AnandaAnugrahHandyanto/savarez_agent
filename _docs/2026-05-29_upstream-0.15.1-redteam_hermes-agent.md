# 2026-05-29 upstream 0.15.1 redteam sync

## Summary

- upstream/main を `28bb7e0a8` まで fetch して、v0.15.1 hotfix と post-release fixes を fork main に取り込み。
- `scripts/sync_all.py` の policy merge で実施。OpenClaw / VRChat / VOICEVOX / OpenCode Zen など独自機能は保持。
- 代表ファイルの存在確認では独自機能ファイルは残存。ええ感じや。
- 上流テストで Windows パスが落ちたため、`agent.image_routing.extract_image_refs()` に Windows drive path 対応を追加。

## Verification

- `.\.venv\Scripts\python.exe -m ruff check .` passed.
- `.\.venv\Scripts\python.exe scripts\run_tests_parallel.py ... -- --tb=short --timeout-method=thread` passed: 19 files, 502 tests.
- dashboard auth tests は `fastapi==0.133.1` が web extra のため、venv に追加して検証。

## Notes

- 初回 test は Windows の `SIGALRM` 非対応で pytest-timeout が落ちた。`--timeout-method=thread` が正解。
- `career_docs_output/` は作業前から未追跡。今回の sync / PR 対象から除外。

## 2026-05-29 follow-up

- upstream/main を追加で `a30480bd2` まで取り込み。compression の同時実行 session-id fork 修正が入ったで。
- `#32314` を調査し、Bedrock の `auth_type="aws_sdk"` が `api_key_env_vars=()` なので AWS credential chain が subprocess blocklist に入らん問題を修正。
- `tools/environments/local.py` で AWS SDK credential env vars を `auth_type == "aws_sdk"` の時に blocklist へ追加。
- 上流 PR 用には `_docs/` を除いて、`tools/environments/local.py` と `tests/tools/test_local_env_blocklist.py` だけ切り出す予定や。

## 2026-05-29 follow-up verification

- `.\.venv\Scripts\python.exe -m ruff check .` passed.
- `.\.venv\Scripts\python.exe scripts\run_tests_parallel.py tests\tools\test_local_env_blocklist.py tests\tools\test_env_passthrough.py tests\tools\test_code_execution_windows_env.py -- --tb=short --timeout-method=thread` passed: 3 files, 64 tests.
- `.\.venv\Scripts\python.exe scripts\run_tests_parallel.py tests\agent\test_bedrock_adapter.py tests\agent\test_bedrock_integration.py tests\hermes_cli\test_bedrock_model_picker.py -- --tb=short --timeout-method=thread` passed: 3 files, 194 tests.

## 2026-05-29 send_message media redteam

- upstream/main は `a30480bd2` から新規 merge なし。公式 PR #34374/#34387 は open/CLEAN のまま。
- open security issue `#34270` を調査。`send_message` の Telegram standalone media path が upload 直前に `validate_media_delivery_path()` を再確認していなかった。
- `_handle_send()` 側の `filter_media_delivery_paths()` は維持しつつ、`_send_telegram()` でも safe path に正規化してから open/upload する二重防御にしたで。
- `python-telegram-bot` optional dep がない venv でも回る専用回帰テストを `tests/tools/test_send_message_telegram_media_validation.py` に追加。

## 2026-05-29 send_message media verification

- `.\.venv\Scripts\python.exe -m ruff check tools\send_message_tool.py tests\tools\test_send_message_tool.py tests\tools\test_send_message_telegram_media_validation.py` passed.
- `.\.venv\Scripts\python.exe scripts\run_tests_parallel.py tests\tools\test_send_message_telegram_media_validation.py tests\tools\test_send_message_tool.py -- --tb=short --timeout-method=thread` passed: dedicated regression 1 passed; optional Telegram suite skipped in this venv.

## 2026-05-29 redact code output redteam

- open security issue `#33801` を調査。`redact_sensitive_text()` 自体は `code_file=True` を持っているのに、`execute_code` と `terminal` の出力 redaction が通常モードで走って `MAX_TOKENS=100` みたいなコード風出力を `MAX_TOKENS=***` に壊していた。
- 既存 PR `#33840` は同じ方向の修正やがテストが見当たらんかったので、こっちは terminal / execute_code の実出力で回帰テストを追加して、挙動で固定したで。
- `file_tools.py` は既に `code_file=True` 済みなので触らず。prefix secret (`sk-...` など) は引き続き redaction されることも確認。

## 2026-05-29 redact code output verification

- `.\.venv\Scripts\python.exe -m ruff check tools\code_execution_tool.py tools\terminal_tool.py tests\tools\test_code_execution.py tests\tools\test_terminal_output_transform_hook.py` passed.
- `.\.venv\Scripts\python.exe scripts\run_tests_parallel.py tests\tools\test_terminal_output_transform_hook.py -- --tb=short --timeout-method=thread` passed: 10 tests.
- `.\.venv\Scripts\python.exe scripts\run_tests_parallel.py tests\tools\test_code_execution.py -- --tb=short --timeout-method=thread -k test_output_redaction_preserves_code_like_env_names` passed: 1 test.

## 2026-05-29 latest upstream catch-up

- upstream/main が `75cd420b3` (`v2026.5.29.2` 周辺) まで進んでいたので、`scripts/sync_all.py --merge --allow-preflight-blockers` で fork main に取り込み。
- 新規 upstream は credential-pool / approval / vision / gateway access policy / optional skills まわりが多め。独自機能は custom-first で維持。
- merge 後に `execute_code` approval-context 修正の import だけ fork 側で落ちて、`propagate_context_to_thread` が `NameError` になった。これは公式差分ではなく fork sync 回帰なので、`tools.code_execution_tool` に upstream と同じ import を復旧したで。

## 2026-05-29 latest upstream verification

- `.\.venv\Scripts\python.exe -m ruff check .` passed.
- `.\.venv\Scripts\python.exe scripts\run_tests_parallel.py tests\tools\test_code_execution.py tests\tools\test_execute_code_approval_cluster.py -- --tb=short --timeout-method=thread -k "test_output_redaction_preserves_code_like_env_names or approval or context"` passed: 18 tests.
