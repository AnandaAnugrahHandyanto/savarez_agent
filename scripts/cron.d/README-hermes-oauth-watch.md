# hermes-oauth-watch cron

`hermes-oauth-watch` runs daily on pi-hermes and posts OpenAI/Codex OAuth renewal warnings to OpenClaw HQ topic 4 when the GPT-5.5 OAuth access JWT stops rotating and approaches expiry.
