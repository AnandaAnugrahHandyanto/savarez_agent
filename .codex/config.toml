# Project-scoped Codex defaults for Motion Granted Citation Database.
# Loaded only when the project is trusted.

approval_policy = "on-request"
sandbox_mode = "workspace-write"
approvals_reviewer = "auto_review"

[agents]
max_threads = 3
max_depth = 1

[windows]
sandbox = "elevated"
