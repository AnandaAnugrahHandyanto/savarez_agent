# Required — copy to .env and fill in
# Never commit .env to git

# Basic auth for homepage protection
# Generate with: htpasswd -nB admin | sed -e s/\\$/\\$\\$/g
HOMEPAGE_AUTH=admin:$$2y$$05$$changeme_generate_real_hash
