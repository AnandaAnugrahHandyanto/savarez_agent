# Required — copy to .env and fill in
# Never commit .env to git

# Basic auth for Traefik dashboard
# Generate with: echo $(htpasswd -nB admin) | sed -e s/\\$/\\$\\$/g
# Example below is admin:changeme
TRAEFIK_DASHBOARD_AUTH=admin:$$2y$$05$$changeme_generate_real_hash
