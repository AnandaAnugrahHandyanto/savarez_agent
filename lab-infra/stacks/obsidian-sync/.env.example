# CouchDB credentials
COUCHDB_USER=admin
COUCHDB_PASSWORD=changeme

# Database name (must match LiveSync plugin config)
COUCHDB_DB=obsidian

# How often to check for vault changes (seconds)
SYNC_INTERVAL=5
